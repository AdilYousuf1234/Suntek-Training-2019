#include<stdio.h>
int fact(int x);
int main()
{
    int n;
    scanf("%d",&n);
    printf("%d",fact(n));
}
int fact(int x)
{
    int fac=x;
    if(x<=1)
    return 1;
    else
    return (fac*fact(x-1));
}

