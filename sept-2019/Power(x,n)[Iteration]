#include<stdio.h>
int power(int b,int p);
int main()
{
    int n,x;
    scanf("%d%d",&n,&x);
    printf("%d",power(n,x));
}
int power(int b,int p)
{
    int i,ans=1;
    for(i=0;i<p/2;i++)
    {
        ans=ans*b;
    }
    if(p%2==0)
    return ans*ans;
    else
    return ans*ans*b;
}
